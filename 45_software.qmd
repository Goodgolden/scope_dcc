---
title: "Software and Packages"
editor: source
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
            warnings = FALSE,
            message = FALSE,
            comment = "#>",
            #results = "hide",
            digits = 4,
            error = FALSE)
```

```{css, echo=FALSE}
.scroll-100 {
  max-height: 300px;
  max-width: 1000px;
  overflow-y: auto;
  background-color: inherit;
}
```

List of statistical software (along with version numbers) to be used for each phase of the
analysis; in the case of R or Stata, additionally list any requisite installed packages and their
version numbers

```{r}
sessionInfo()
```

