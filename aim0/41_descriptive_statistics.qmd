---
title: "Descriptive Statistics"
editor: source
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
            warnings = FALSE,
            message = FALSE,
            comment = "#>",
            #results = "hide",
            digits = 4,
            error = FALSE)

```

```{css, echo=FALSE}
.scroll-100 {
  max-height: 300px;
  max-width: 1000px;
  overflow-y: auto;
  background-color: inherit;
}
```

List of characteristics (e.g. demographic, clinical) to be summarized descriptively (e.g. “Table
1”)
Description of how these characteristics will be summarized descriptively (e.g. means/medians
vs. N (%), tabular displays, graphical displays, etc.)
Summarize follow-up time (e.g. average and total amount) and number of events